<span ng-if="computeProgress === 1.0" class="compute-progress">
  <i class="glyphicon glyphicon-ok" drop-tooltip="already computed"></i></span>
<span ng-if="computeProgress === 0.5" class="compute-progress">
  <i class="glyphicon glyphicon-flash" drop-tooltip="computation started"></i></span>
