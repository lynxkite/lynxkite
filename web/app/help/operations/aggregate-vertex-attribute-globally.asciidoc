## Aggregate vertex attribute globally

Aggregates vertex attributes across the entire graph into one scalar for each attribute.
For example you could use it to calculate the average age across an entire dataset of people.

====
[[prefix]] Generated name prefix::
Save the aggregated values with this prefix.

include::glossary.asciidoc[tag=global-aggregators]
====
